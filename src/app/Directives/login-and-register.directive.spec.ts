import { LoginAndRegisterDirective } from './login-and-register.directive';

describe('LoginAndRegisterDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginAndRegisterDirective();
    expect(directive).toBeTruthy();
  });
});
